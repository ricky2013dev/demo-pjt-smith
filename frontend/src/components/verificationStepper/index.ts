/**
 * The three-step verification progress indicator, shared by Patient Detail and
 * the Patient List table via its `layout` prop.
 */
export { default as VerificationStepper } from './VerificationStepper';
